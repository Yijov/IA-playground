{
  "name": "y",
  "version": "1.0.0",
  "description": "newral network",
  "main": "index.js",
  "scripts": {
    "build": "tsc && npm run copy-html && npm run copy-brains",
    "dev": "tsc --watch && npm run copy-html && npm run copy-brains",
    "copy-html": "copyfiles -u 1 src/index.html dist",
    "copy-brains": "copyfiles -u 2 src/brains/**/* dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yirbett Joseph",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.8.3"
  }
}
